# Weather Forecast Web App

A clean and responsive weather dashboard built with **HTML, CSS, and JavaScript**.  
The app allows users to search for any city and view its current weather conditions using the **OpenWeather API**.

## 🌦 Features

- Search weather by **city name**
- Shows:
  - Location (City, Country)
  - Weather condition (e.g., Haze, Clear, Clouds)
  - Current temperature
  - Feels like temperature
  - Today’s high & low
  - Humidity
  - Wind speed
- Modern card-based UI with soft shadows and gradients
- Responsive layout for different screen sizes
- (Optional) Saves recent search in browser storage for quick access

## 🛠 Tech Stack

- **HTML5** – Structure and layout  
- **CSS3** – Styling, gradients, responsiveness  
- **JavaScript (Vanilla JS)** – API calls, DOM updates, logic  
- **OpenWeather API** – Real-time weather data  

## 🚀 Getting Started

### 1. Clone the repository

```bash
git clone https://github.com/your-username/your-repo-name.git
cd your-repo-name
```
2. Get an OpenWeather API key

Go to OpenWeather

Create an account and generate an API key

Copy your API key

3. Add your API key

In your script.js (or equivalent JS file), replace the placeholder with your key:
```bash
const API_KEY = "YOUR_API_KEY_HERE";
```
4. Run the project

You can simply open index.html in your browser, or use a live server extension (like VS Code Live Server) for a better experience.
Project Structure
```bash
├── index.html      # Main page
├── style.css       # All styles
└── script.js       # JavaScript logic (API calls, DOM updates)
```
<img width="571" height="385" alt="image" src="https://github.com/user-attachments/assets/7c19c069-3330-4cbf-8866-f8b942f0c41b" />
